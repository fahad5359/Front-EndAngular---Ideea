export class Useer{
  // we put our varibles into this constuctor , so we can share/export these varibles to another componets.
  userName: string;
  passWord:string;
  ideea:string
  email:string
  subject:string
  constructor(userName:string, passWord:string, ideea:string, email:string, subject:string) {}
}
