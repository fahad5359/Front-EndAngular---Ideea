export class Useer{
  // we put our varibles into this constuctor , so we can share/export these varibles to another componets.
  userNem: string;
  passWord:string;
  ideea:string
  email:string
  subject:string
  constructor(userNem:string, passWord:string, ideea:string, email:string, subject:string) {}
}
